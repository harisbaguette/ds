# 패토브 화면 뼈대

2026-09-24. 최신 사용자 지시를 기준으로 외부 구조를 다시 줄였다.
같은 날 어색한 부분을 재검수해 "낮은 입체감의 작업 패널"로 정리했다.
상세 내용과 내부 부품 가공은 뼈대 확정 뒤 진행한다.

## 목표와 기본 흐름

디자인 패턴 사전 → 스타일별 디자인 시스템 → 필요한 부품을 가져와 일관되게 조합.
사용자는 한 프로젝트에 한 스타일을 고른다. 사이트는 그 스타일의 필요한 패턴을 찾는 일을 돕는다.

**스타일 입구 → 선택한 스타일의 그리드 → 패턴 상세.**

첫 화면은 선화·컬러블록·풍경의 시각 표지 세 개다. 스타일을 고르면 목록 전체에 같은 스타일을 적용한다.
검색·분류를 한 뒤 스타일을 바꿔도 찾고 있던 조건을 유지한다.
저장은 보조 동작이며, 저장한 항목도 스타일별로 묶는다.

## 남길 것과 뺀 것

| 남김 | 이유 |
|---|---|
| 스타일 표지·현재 스타일 선택 | 한 벌의 디자인 시스템을 고르고 유지하는 핵심 |
| 패턴 그리드·검색·분류 | 필요한 부품을 눈으로 찾는 데 직접 필요 |
| 상세·닫기·뒤로 가기 | 선택한 패턴을 살피고 이전 위치로 돌아감 |
| 보조 저장·해제·되돌리기 | 다시 찾을 항목을 보관, 실수 복구 |

패턴끼리 비교, 비교 선택·도크·대화상자, 리스트 보기, 보기 전환, 정렬,
큰 제목·개수 배지, 로고 워드마크, 사이드바 접기, 모바일 중복 주 메뉴·메뉴 서랍,
상세 안 스타일 전환, 스타일 표지의 장면 전환은 제거했다.
이전 작업에 있던 기능이라는 이유로 복구하지 않는다.

## 외부 시각·조작 기준

- 로고는 기존 mark.svg만 표시한다. 접근성 이름은 유지한다.
- 사이트 제목은 16px. 외부 설명문·히어로·부제는 두지 않는다.
- 스타일 입구는 데스크톱 3열, 모바일 1열. 표지는 기존 스타일 장면을 재사용한다.
- 스타일 안에서는 176px 분류 사이드바와 그리드다. 사전·구성요소·문서는 244px 문서형 사이드바를 쓴다. 모바일은 가로 분류줄이나 선택 상자로 바뀐다.
- 검색은 라이브러리와 저장 화면에만 둔다. 검색 제안은 실제 로컬 일치 항목이다.
- 저장 버튼은 데스크톱에서 호버·초점·저장된 상태일 때 드러난다. 터치 화면에서는 항상 보인다.
- 저장 버튼은 카드 설명 줄 안에 붙는다. 버튼이 카드 밖에 떠서 어떤 항목의 저장인지 헷갈리면 실패다.
- 모바일 주 메뉴는 아이콘과 글자를 함께 보여 현재 위치를 바로 알게 한다.
- 입체감은 헤더·사이드바·카드·눌림 상태의 네 수준 안에서만 쓴다. 그림자가 서로 겹쳐 위계를 흐리면 실패다.
- 조작 영역 최소 44px, 키보드 초점, 메뉴 Escape, 상세 초점 가두기·복귀를 유지한다.
- 외부 표면은 밝은 회색의 낮은 입체감으로 통일한다. 색상과 큰 시각 요소는 스타일 표본과 패턴에 집중한다.

표지는 비조작 예시를 일정 비율로 축소해 보여 준다. 실제 클릭 대상은 표지 전체다.
패턴 그리드 안의 예시는 크기별 정보량을 조정하는 기존 방식을 유지하며 전체를 축소하지 않는다.
작은 글자는 조작 라벨·보조 정보·비조작 표본이며 상세 본문 기본값은 16px이다.

## 구현 경계

정적 HTML/CSS/JS. 별도 빌드 없이 파일 실행과 로컬 HTTP 실행을 지원한다.
화면은 src/ui/views.js, 탐색·저장은 src/app.js, 외부 CSS는 shell.css·dialogs.css다.
내부 previews.js·previews.css와 catalog.js의 원본은 이번에도 수정하지 않았다.
스타일 표지에서 기존 장면의 할 일 하단만 제외하는 외부 래퍼를 사용한다.
브라우저 저장은 pattove-shell:v1 키를 유지한다. 옛 목록·비교 URL은 현재 그리드 주소로 정리한다.

전체 사전·본문·실동작 컴포넌트·코드 배포는 후속 범위다. 현재 12개 패턴은 외부 뼈대 검토용이다.
기능을 줄인 것은 사이트의 조작 범위이며, 정의 문서가 정한 전체 사전의 수록 목표를 줄인 것이 아니다.

## 2026-09-24 다듬기 수용 기준

- Given 320·375px 화면에서 주 메뉴를 볼 때, When 네 메뉴와 저장 버튼이 보이면, Then 각 메뉴 이름이 보이고 서로의 클릭 영역을 가리지 않는다.
- Given 패턴 카드 목록에서 저장 버튼을 볼 때, When 카드 위·초점·터치 화면 상태가 되면, Then 저장 버튼은 해당 카드의 설명 줄 안에 붙어 보인다.
- Given styles, patterns, saved, components, dictionary, docs, detail, empty/search 8개 흐름을 볼 때, When 320·375·768·1440px로 확인하면, Then 가로 넘침·글자 겹침·의미 없는 장식 배지가 없다.

[시안·비판·구현 검수](문서/패토브-스타일-우선-검수.md) · [사용자 브리프](문서/패토브-디자인-브리프.md)
