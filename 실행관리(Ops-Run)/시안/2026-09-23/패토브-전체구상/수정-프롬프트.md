# 이미지 수정 프롬프트

첫 생성에서 발견한 과잉 설명과 배경의 비일관성을 수정했다. 내장 image_gen 편집을 사용하며 원본 생성 파일은 보존한다.

## 02

Edit this existing PATTOVE UI concept precisely. Keep its logo, white shell, overall alignment, search query 저장 완료, 3 result tiles, selected first two tiles and comparison function. Correct the regression in information density: REMOVE all the gray 피드백 badges and ALL explanatory paragraphs underneath the three pattern titles. Do not replace them with any new text. Each tile must contain only a spacious visual miniature, the short title and compare checkmark. Remove the outer tall card box around the title; only the preview stage is lightly bounded, as in the second reference. Put three titles directly below preview areas: 잠깐 알림, 인라인 안내, 확인창. The first preview should show a tiny subtle transient toast on a document, the second an inline reading-permission notice '읽기 전용 문서' with '권한 요청', the third a proper destructive confirmation '컬렉션을 삭제할까요?' with 취소 and 삭제, not a modal about saved success. Use short actual document text '브랜드 가이드', '색과 형태', '팀 공유' instead of gray skeleton lines where possible. Compact the bottom comparison tray into a centered floating bar about 55% of canvas width, with 2개 선택, the two removable names, and 비교. Maintain ample white space. Exact Korean, no new subtitles, no category badges or descriptions. Improve precision but preserve the approved lowercase pattove logo.

## 03

Make one surgical correction to image 1, an otherwise approved PATTOVE pattern-detail mockup. PRESERVE the entire foreground Toast detail modal exactly: its header, demo, Korean text, right-side usage guidance, footer and controls. REPLACE ONLY THE BACKGROUND APP BEHIND THE MODAL with the PATTERN GALLERY shown in image 2. The current background incorrectly shows the styles page and inspiration thumbnails. It must instead show the neutral dictionary gallery, top navigation 패턴 selected, narrow left sidebar 전체 16 / 이동 / 입력 / 탐색 / 피드백 / 복구 and rows of pale component miniatures. No style timer panels or inspiration thumbnails behind it. Keep the background gently dimmed and subordinate, with identical logo placement and shell scale. Do not alter brand, foreground text, accent colors, whitespace or the composition of the modal. No added labels or slogans.

## 04

Edit image 1, the PATTOVE two-pattern comparison. Preserve the foreground comparison modal, both preview examples, all four comparison rows and both 상세 보기 actions. Correct ONLY the app shell visible behind the modal to match image 2 exactly. The current background invents 248 items, 24 alerts and new category names. Remove that fabricated background content. Correct sidebar is 전체 16 / 이동 2 / 입력 2 / 탐색 4 / 피드백 5 / 복구 3. Correct page title is 전체 16. Correct search placeholder is 이름이나 상황 검색. Correct top tabs are 패턴 / 스타일 / 저장 and 패턴 selected. Main background is a three-column pale neutral pattern gallery. Preserve the split cobalt P and navy lowercase pattove logo. Keep background slightly dimmed, no giant blur. No new counts, tags, paragraphs or other additions. The foreground readable comparison remains the main focus.

## 07

Edit this exact PATTOVE saved-patterns desktop UI screenshot surgically. Preserve the entire existing visual design, white shell, blue split-P logo, typography, navigation, spacing, all six saved entries, their names/styles, and the rest of the imagery. Three specific content corrections only:
1. Bottom CENTER preview is titled '진행률' and has style '컬러블록' but currently depicts a three-step form wizard. Replace the wizard completely with a real file upload progress example on the same cobalt-blue canvas: a compact geometric acid-lime file symbol, filename 'brand-assets.zip', small '24.8 MB', a bold '68%' and a horizontal progress bar filled 68% in acid lime, with a muted blue remainder. Crisp flat vector geometry, no gradients. No step labels or circles.
2. Bottom RIGHT empty-state preview is a demo inside a saved gallery, so replace '저장한 패턴이 없어요.' with '아직 비어 있어요.' and its button label with '+ 새 컬렉션'. Keep the beautiful calm landscape and tiny sleeping cat unchanged.
3. Top RIGHT confirmation demo heading should read '컬렉션을 삭제할까요?' instead of the vague existing heading. Keep 취소 and 삭제 buttons.
Every other pixel/composition should stay as close to the supplied image as possible. Render a refined high-resolution front-on UI screenshot. No extra text or subtitles.

## 08

Edit this exact PATTOVE mobile three-screen concept image surgically. Preserve the layout, white background, all three flat mobile screen frames, blue split-P logo, Korean navigation, left discovery screen, center detail sheet, right styles screen, all spacing and typography. Correct two semantic content mistakes only:
1. CENTER mobile screen: the light-gray demo stage currently shows an upper large success toast, then a black '저장' button, then a smaller result toast. Replace ONLY the upper large success toast with a simple white note content card: heading '팀 회의 메모', one restrained line '화요일 오후 2시', and two short neutral gray text-rule lines beneath. No success icon and no dismiss X in that content card. Keep the black '저장' demo button in exactly its current position, and keep the single smaller lower success toast '문서가 저장됐어요.' This must clearly be a note -> save action -> one feedback result.
2. RIGHT mobile screen: the top of the first line-art timer preview currently repeats the style name '선화'. Replace the INNER top-left '선화' heading above the timer with '집중'. Preserve the OUTER caption '선화' below that timer and its 적용 action. The three small color dots at the inner top right should remain.
Preserve everything else, including the pattern-save footer, style choices, crisp Korean text, thin black line-art cat, large 25:00 timer, and partially visible cobalt/lime style below. High-resolution polished UI concept, no extra subtitles or annotations.
